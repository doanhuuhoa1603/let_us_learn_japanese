import 'package:flutter/material.dart';

class Grammar extends StatelessWidget {
  const Grammar({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Ngữ pháp');
  }
}